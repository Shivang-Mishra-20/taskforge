# Task Forge AI — Dependencies (all free/open-source)

# Core
pydantic>=2.5.0,<3.0.0
pyyaml>=6.0.1

# HTTP server for HF Spaces (port 7860)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# OpenAI-compatible client (optional — falls back to rule-based if no key)
openai>=1.30.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Utilities
typing-extensions>=4.10.0
